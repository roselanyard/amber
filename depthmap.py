import numpy
import sharedvars

local_image = numpy.zeros((sharedvars.m, sharedvars.n))
def update_depth_map():
    pass
    # while not sharedvars.exiting:

        # with sharedvars.amplitudes_lock():
        #    local_image = camera.get_greyscale_frame()
